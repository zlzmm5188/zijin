# 前台总账号信息

## 📋 账号信息

### 总账号（用于生成邀请码）

**邀请码(UID)**: `88888888`

**用户名**: `admin888`

**密码**: `password123`

**手机号**: `13888888888`

**邮箱**: `admin888@providence.com`

**VIP等级**: `6`（最高等级）

**状态**: `正常`

**推荐人**: `无`（顶级账号，parent_id = NULL）

---

## 🎯 用途说明

### 1. 生成邀请码

- 总账号的邀请码是：**88888888**
- 新用户注册时，使用此邀请码即可关联到总账号
- 所有通过此邀请码注册的用户，`parent_id` 都会指向总账号

### 2. 后台查询

**查询总账号名下的所有注册用户**：
```sql
SELECT * FROM users WHERE parent_id = (SELECT id FROM users WHERE uid = '88888888');
```

**查询总账号名下的所有充值记录**：
```sql
SELECT r.*
FROM recharge_orders r
INNER JOIN users u ON r.user_id = u.id
WHERE u.parent_id = (SELECT id FROM users WHERE uid = '88888888')
ORDER BY r.created_at DESC;
```

**查询总账号名下的所有投资记录**：
```sql
SELECT o.*
FROM invest_orders o
INNER JOIN users u ON o.user_id = u.id
WHERE u.parent_id = (SELECT id FROM users WHERE uid = '88888888')
ORDER BY o.created_at DESC;
```

---

## 📊 邀请链接

### 方式1：使用邀请码
```
https://88888888.copla.top/
```

### 方式2：直接使用邀请码
新用户注册时，填写邀请码：`88888888`

---

## 🔍 后台查询示例

### 查询总账号信息
```sql
SELECT
    id,
    uid AS '邀请码',
    username AS '用户名',
    phone AS '手机号',
    vip_level AS 'VIP等级',
    status AS '状态'
FROM users
WHERE uid = '88888888';
```

### 查询总账号下级用户数量
```sql
SELECT COUNT(*) AS '下级用户数'
FROM users
WHERE parent_id = (SELECT id FROM users WHERE uid = '88888888');
```

### 查询总账号下级用户列表
```sql
SELECT
    u.id,
    u.uid AS '邀请码',
    u.username AS '用户名',
    u.phone AS '手机号',
    u.vip_level AS 'VIP等级',
    u.total_invest AS '累计投资',
    u.created_at AS '注册时间'
FROM users u
WHERE u.parent_id = (SELECT id FROM users WHERE uid = '88888888')
ORDER BY u.created_at DESC;
```

### 查询总账号下级用户充值总额
```sql
SELECT
    SUM(r.amount) AS '充值总额',
    COUNT(*) AS '充值次数'
FROM recharge_orders r
INNER JOIN users u ON r.user_id = u.id
WHERE u.parent_id = (SELECT id FROM users WHERE uid = '88888888')
AND r.status = 2;  -- 2=已完成
```

---

## ⚠️ 重要提示

1. **密码安全**：建议首次登录后立即修改密码
2. **账号保护**：此账号作为总账号，请妥善保管
3. **权限说明**：此账号是前台普通账号，不是后台管理员账号
4. **邀请码使用**：所有新用户都可以使用 `88888888` 作为邀请码注册

---

## 🔧 修改密码（如果需要）

如果需要修改密码，可以使用以下SQL：

```sql
-- 生成新密码的bcrypt哈希（密码: newpassword）
-- 可以使用PHP的 password_hash() 函数生成
UPDATE users
SET password = '$2y$10$新密码的bcrypt哈希值'
WHERE uid = '88888888';
```

或者通过前台登录后修改密码。

---

**创建时间**: 2025-01-XX
**账号状态**: ✅ 已创建并激活
