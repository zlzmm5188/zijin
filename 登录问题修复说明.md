# 登录问题修复说明

**问题**: 无法登录
**原因**: `login.js` 中API地址和路径错误
**修复时间**: 2025-01-XX

---

## 问题分析

### 1. API地址错误
- **错误**: `https://api.frevix.top`
- **正确**: `https://apis.copla.top`

### 2. API路径错误
- **错误**: `/api/login.php`
- **正确**: `/login/login/account`

---

## 已修复内容

### 修复的文件
- ✅ `/www/wwwroot/xin.frevix.top/providence/login.js`

### 修复的代码行
1. **Line 148** - 登录API调用
   - 旧: `API_BASE + '/api/login.php'`
   - 新: `API_BASE + '/login/login/account'`

2. **Line 148, 222, 273, 318** - API_BASE默认值
   - 旧: `'https://api.frevix.top'`
   - 新: `'https://apis.copla.top'`

---

## 正确的登录API

### API路径
```
POST https://apis.copla.top/login/login/account
```

### 请求参数
```json
{
  "username": "用户名或手机号",
  "password": "密码"
}
```

### 成功响应
```json
{
  "code": 1,
  "message": "登录成功",
  "data": {
    "access_token": "token字符串",
    "user_id": 123,
    "username": "用户名"
  }
}
```

### 失败响应
```json
{
  "code": -1,
  "message": "用户不存在或密码错误"
}
```

---

## 测试步骤

### 1. 清除浏览器缓存
- 按 `Ctrl + Shift + R` (Windows/Linux)
- 或 `Cmd + Shift + R` (Mac)

### 2. 刷新登录页面
```
https://copla.top/login.html
```

### 3. 使用测试账号登录
- 使用已注册的账号和密码
- 点击"登录"按钮

### 4. 检查浏览器控制台
- 按 `F12` 打开开发者工具
- 切换到 "Console" 标签
- 查看是否有错误信息

---

## 故障排查

### 如果仍然无法登录

#### 检查1: API地址是否正确
在浏览器控制台（F12）中输入：
```javascript
console.log(window.API_CONFIG?.baseURL);
```
应该显示：`https://apis.copla.top`

#### 检查2: 网络请求是否成功
1. 打开开发者工具（F12）
2. 切换到 "Network" 标签
3. 刷新页面并尝试登录
4. 查看登录请求的状态码
   - 200: 请求成功
   - 404: 接口不存在
   - 500: 服务器错误

#### 检查3: 后端API是否正常
访问以下URL测试API是否可访问：
```
https://apis.copla.top
```

#### 检查4: 数据库连接是否正常
检查后端API的数据库配置：
```php
// /www/wwwroot/xin.frevix.top/providence-admin/config/database.php
```

---

## 相关文件

- `/www/wwwroot/xin.frevix.top/providence/login.js` - 登录逻辑
- `/www/wwwroot/xin.frevix.top/providence/login.html` - 登录页面
- `/www/wwwroot/xin.frevix.top/providence/config.js` - API配置
- `/www/wwwroot/xin.frevix.top/providence-admin/api/login.php` - 后端登录API

---

## 注意事项

1. 修改后需要清除浏览器缓存
2. 确保站点配置正确：
   - 前台: `copla.top`
   - 接口: `apis.copla.top`
3. Token存储键名为 `providence_token`

---

**修复完成！现在应该可以正常登录了。**
